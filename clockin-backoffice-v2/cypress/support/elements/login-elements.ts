export enum LoginElements {
  email = "#email",
  password = "#password",
  botaoLogin = 'button[type="submit"]',
  gateError = ".gate-error",
  forgotPassword = ".gate-nav",
  loginToCarol = ".gate-btn",
  environment = "#tenant",
  organization = "#organization",
  resetOrganization = ":nth-child(1) > .wrap--w-full > .side-content--right > .cw-btn-subtle",
}
